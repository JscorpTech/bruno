meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{domain}}/api/oauth/logout
  body: none
  auth: bearer
}

headers {
  Accept-Language: {{language}}
}

auth:bearer {
  token: {{token}}
}

meta {
  name: Get compilations
  type: http
  seq: 1
}

get {
  url: {{domain}}/api/compilations
  body: none
  auth: bearer
}

headers {
  Accept-Language: {{language}}
  X-Application-Token: {{local_token}}
}

auth:bearer {
  token: {{token}}
}
